/**
 * The driver surface. See the factory in `vite.config.ts` for why the surface is
 * a config file rather than an environment variable or a `--mode`.
 */
import { createViteConfig } from './vite.config';

export default createViteConfig('driver');
